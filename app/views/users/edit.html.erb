<h1>Profile編集</h1>

<%= form_with model: @user, local: true do |f| %>
Name<%= f.text_field :name, autofocus: true, id: "inputname", placeholder: "名前" %>

Profile画像<%= f.file_field :profile_image,  placeholder: "Profile画像", accept: "image/*" %>

自己紹介<%= f.text_area :introduction, autofocus: true, accept: "image/*" %>

<%= f.submit "変更を保存" %>

<% end %>